
how to pull the docker container
docker pull centos

how to run container
docker run centos

how to see downloaded container
docker images

Step 5 — Running a Docker Container
docker run -it centos
(it will itrative mode)

how to install application in container

yum install mariadb-server

how to exit container 
exit

how to commit the changes in container
docker commit -m "What did you do to the image" -a "Author Name" container-id repository/new_image_name

Ex: docker commit -m "added mariadb-server" -a "Sunday Ogwu-Chinuwa" 59839a1b7de2 finid/centos-mariadb

Step 7 — Listing Docker Containers

how to see all active and inactive all container 
docker ps

only active container see
docker ps -a

veiw latest container 
docker ps -l

how to stop container force fully remove (-f)
docker stop container-id

how to run container in dettach mode (-d)
docker run -d nginx

how to assign name of the container --name c1
docker run --name -d nginx

how to list all container id 
echo $(docker ps -aq)

we want remove all container all 
docker rm -f $(docker ps -aq)

howm to remove images
docker rmi imageid/imagename

direct run 
docker run --name -d nginx

how to see log 
docker logs -f(live log) c1


Step 8 — Pushing Docker Images to a Docker Repository

docker login -u docker-registry-username

docker push docker-registry-username/docker-image-name

how to use exec --name c1 nginx
exit from shell ctrl+pq

how to check ip add
docker inspect nginx
all list details docker

how to check https isworking or not
curl 172.17.0.2:80

docker port mapping 
docker run --name c2 -p 9090:80 -d nginx
automatic port mapping we are using (-P)
nano 
we need delete all images prune-a cmd
docker image prune -a (all)
it will delete all images atleatest one conttainer associates with them.

exsitng container inside we can go
docker exec -it c4 bash 

how to pass environment vaiables in docker
docker run --name mydb -d -e MYSQL_ROOT_PASSWORD=abc mysql:5

to got container inside
docker exec -it mydb bash

then login mysql 
mysql -u root -p

password =abc

show Databases;

to exit ctrl=pq

how to link container with mydb (--link name 0f container)
docker run --name wordpress -d -P --link mydb wordpress

then access in browser http://192.168.18.5:32768/wp-admin
then it will ask db details and host fill it will accpet

then set wordpress credetial and login

busybox- which combines tiny versions  unix multiple into a single small executalbe

 docker run --name busybox1 -it busybox
docker run --name busybox2 --link busybox1:busybox1-alias -it busybox

networking of container
1. bridge 2. host 3. none

docker inspect bridge

by default all container is connected with bridge network.
default address : 172.17.0.2/16
g/w 172.17.0.1/16




